# three node (two workers) cluster config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  image: {{ .Values.digest }} 
- role: worker
  image: {{ .Values.digest }}
- role: worker
  image: {{ .Values.digest }}

